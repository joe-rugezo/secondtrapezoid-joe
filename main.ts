game.splash("let us calculate the area of the trapezoid")
let length = game.askForNumber("what is the length")
let width = game.askForNumber("what is the width")
let height = game.askForNumber("what is the height")
let area = length + width
area = 30 / 2
area = 15 * height
game.splash("The area of a trapezoid with length" + length + "cm, width" + width + "cm and height" + height + "cm is" + area + "cm^2")
